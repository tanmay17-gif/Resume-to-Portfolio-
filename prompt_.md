Profile: When I enter my name and click Save Changes, the changes are not being saved. Fix this so that the updated details are actually saved.
Settings: The Settings section is too basic. Add useful and functional elements that websites usually have in Settings, with proper working functionality.
Help: The Settings and Help sections currently have a Close button. Remove it because the interface already has an X/Cross button to close the panel.

The main issue is that there are currently multiple pages such as Dashboard, Review, Upload, and Style, which are overriding each other and making the workflow complicated. I want the entire working flow to be handled through one single chat interface, similar to how ChatGPT or Claude works, but not an exact copy. Remove unnecessary pages and components that are no longer required.

Required Flow:

New Session → Chat Interface

In a new session, the chat interface should have an Upload Resume button.
Add a Style dropdown beside/within the chat interface.
The Style dropdown should have the complete functionality of the current Style page's style list, including the style options and hover previews.
The user must upload a resume/file before proceeding further.
Processing & Resume Extraction
Once the resume is uploaded, start extracting the data.
During extraction, show the processing stages inside the chat interface.
I specifically want the checklist/tick-style processing animation currently used on the Style page, where each stage gets completed one by one with a tick.
For example, the stages can be shown as the system processes the resume, and once each stage is completed, it should be marked as done.
Review & Edit
Once the extraction is completed, the chat should ask the user to review and edit the extracted data.
When the user clicks the review/edit option, open the Review & Edit popup/modal as shown in the reference image.
The user should be able to review, edit, add, and delete information directly inside this popup.
Once the user confirms/proceeds, continue to the next step.
Portfolio Generation
After the review is completed and the style is selected, start processing the portfolio based on the extracted/reviewed data and selected style.
During portfolio generation, show the processing stages inside the chat, completing them one by one so the user can see what the system is doing.
Once the processing is complete, show the generated portfolio preview directly inside the chat window.
Main Requirement

Everything should happen inside one chat interface:

New Session → Upload Resume → Select Style → Extract Data → Processing Checklist → Review & Edit Popup → Portfolio Processing → Processing Stages → Portfolio Preview

The user should not have to move between separate Dashboard, Upload, Review, and Style pages for this workflow. Remove unnecessary pages, components, and duplicate functionality that are overriding each other.

The overall experience should feel like a single intelligent workspace, similar to the conversational workflow of ChatGPT/Claude, but with its own unique design and functionality.

UI/UX

The current typography and UI are very basic and not appealing. Improve the entire interface with a unique, enhanced, professional, and premium UI.

Use better typography, spacing, hierarchy, cards, buttons, icons, animations, and micro-interactions.
Improve the morphism and visual depth.
Make it modern and polished without making it overly flashy.
Avoid the generic AI-generated look.
Make it feel like a real professional product with a distinctive visual identity.
Ensure the entire interface is fully responsive across desktop, tablet, and mobile.
Keep the design consistent throughout the complete chat-based workflow.
Design Style Reference

Use the attached reference image as the primary visual inspiration for the UI/UX design language. Do not copy the exact layout, content, or structure of the reference. Only take inspiration from its visual style and apply that style to the existing application structure and workflow described above.

The design style can be described as Minimalist Editorial / Fashion Editorial Web Design with a Swiss-inspired Grid and Asymmetric Layout.

The UI should incorporate the following characteristics from this style:

High-fashion editorial aesthetic rather than a typical SaaS or AI dashboard.
Clean, sophisticated, and minimal visual language.
Asymmetric grid-based layouts with intentional positioning and strong visual balance.
Large, bold editorial typography combined with smaller refined supporting text.
Strong use of whitespace and negative space.
Thin, delicate borders and divider lines.
Muted, sophisticated colors such as off-white, ivory, soft grey, muted blue/blue-grey, charcoal, and other restrained tones.
Avoid the common purple-and-black AI aesthetic.
Use color selectively rather than filling the entire interface with gradients.
Use a combination of large typography, small uppercase labels, subtle metadata, and clean navigation elements.
Introduce subtle horizontal/vertical lines and grid structures to create an editorial composition.
Use carefully controlled asymmetry so the interface feels artistic but still remains easy to use.
Use large visual areas and content blocks without making the interface feel crowded.
Cards and panels should feel flat, refined, and layered, rather than heavily rounded or overly glossy.
Use subtle morphism / soft surface depth through very light shadows, thin borders, and slight elevation.
Avoid excessive glass transparency, heavy blur, neon effects, glowing borders, or excessive gradients.
Use elegant and restrained animations that support the interaction rather than distract from it.
Hover interactions should feel smooth and premium.
The overall interface should feel like a combination of a high-end fashion/editorial website and a modern intelligent creative workspace.
The visual design should remain functional and readable even with the editorial/asymmetric aesthetic.

Most importantly, do not change the existing application structure or workflow to imitate the reference image. The reference is only for the visual design direction. Keep the required chat-first workflow exactly as defined above and apply this editorial design language to it.

The final result should feel unique, premium, editorial, modern, professional, and highly polished, while still being intuitive and responsive.